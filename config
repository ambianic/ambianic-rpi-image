# pi-gen config file for Ambinic Edge image build
# by default pi-gen prefixes build images with today' date
# we prefer to have a stable name, so we override the date variable below
# with part of the image name, which is a workaround but it gets the job done.
IMG_DATE="ambianic_edge"
IMG_NAME="rpi"
STAGE_LIST="stage0 stage1 stage2 stage3_homebridge"
RELEASE=buster
DEPLOY_ZIP=1
LOCALE_DEFAULT=en_US.UTF-8
TARGET_HOSTNAME=ambianic-edge
KEYBOARD_KEYMAP=us
KEYBOARD_LAYOUT="English (US)"
TIMEZONE_DEFAULT=America/Chicago          
FIRST_USER_NAME=ambianic-edge
FIRST_USER_PASS="ChangeMeOnFirstLogin"
# build only raspberry pi os lite image stages without desktop stages
STAGE_LIST="stage0 stage1 stage2 pi-gen-stage-ambianic"
ENABLE_SSH=1
